## 1. Compatibility and model identification content

- [x] 1.1 Expand `README.md` with a `Supported brands and models` section that clearly separates known-compatible and unverified coverage.
- [x] 1.2 Add a `How to identify your model` section with at least two methods (for example: physical label/settings UI and discovery metadata fields).

## 2. Setup and usage onboarding

- [x] 2.1 Add a `Prerequisites` section aligned with current runtime and environment requirements.
- [x] 2.2 Add a `Quick start` flow and examples that cover device discovery and one common control workflow using current public APIs.

## 3. Troubleshooting and validation

- [x] 3.1 Add a `Troubleshooting` section for common discovery, capability, and wake/connectivity failures with practical remediation steps.
- [x] 3.2 Add a GitHub issue template for diagnostics collection and link to it from troubleshooting.
- [x] 3.3 Validate README command/code snippets against current repository scripts and exported APIs.
- [x] 3.4 Proofread the updated README for clarity, consistency, and markdown rendering quality.
